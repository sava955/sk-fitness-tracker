import { DsInfiniteScrollDirective } from './ds-infinite-scroll.directive';

describe('DsInfiniteScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new DsInfiniteScrollDirective();
    expect(directive).toBeTruthy();
  });
});
